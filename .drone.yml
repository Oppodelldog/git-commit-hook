workspace:
  base: /go
  path: src/github.com/Oppodelldog/git-commit-hook

pipeline:
  build:
    image: oppodelldog/git-commit-hook:latest
    pull: true
    commands:
      - pwd
      - ls -al
      - make ci